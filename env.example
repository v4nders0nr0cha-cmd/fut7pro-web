# ===== Fut7Pro - Frontend (.env.example) =====
# Copie para .env em dev/CI

# API base (ambiente de testes/homolog)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Multi-tenant (slug do racha padrão para desenvolvimento)
NEXT_PUBLIC_DEFAULT_RACHA_SLUG=fut7pro

# Feature flags básicas
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_MOCKS=false

# Se usar NextAuth no frontend público:
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=replace-me