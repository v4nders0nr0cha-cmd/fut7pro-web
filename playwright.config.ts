import type { PlaywrightTestConfig } from "@playwright/test";const DEFAULT_BASE = `http://127.0.0.1:3000`;const baseURL = process.env.PLAYWRIGHT_BASE_URL || DEFAULT_BASE;const config: PlaywrightTestConfig = {  testDir: "./tests",  timeout: 30_000,  expect: { timeout: 5_000 },  fullyParallel: true,  retries: process.env.CI ? 2 : 0,  workers: process.env.CI ? 2 : undefined,  use: {    baseURL,    trace: "retain-on-failure",    screenshot: "only-on-failure",    video: "retain-on-failure",  },  // Agora o Playwright apenas inicia o servidor (o build roda antes, no script test:e2e)  webServer: {    command: "npm run e2e:start",    url: baseURL,    reuseExistingServer: !process.env.CI,    timeout: 120_000,    stdout: "pipe",    stderr: "pipe",  },  projects: [    { name: "chromium", use: { browserName: "chromium" } },    { name: "firefox", use: { browserName: "firefox" } },    { name: "webkit", use: { browserName: "webkit" } },  ],};export default config;